import { CustomDateFormat1Directive } from './custom-date-format1.directive';

describe('CustomDateFormat1Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomDateFormat1Directive();
    expect(directive).toBeTruthy();
  });
});
